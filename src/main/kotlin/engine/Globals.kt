package engine

import engine.toolkit.SpriteLoader

object Globals {
    var DEBUG_MODE = true

    var WINDOW_WIDTH = 0
    var WINDOW_HEIGHT = 0

    var DRAW_BLOCKRECT = false

    var spriteLoader: SpriteLoader? = null
}